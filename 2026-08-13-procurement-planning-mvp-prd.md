# 기기·옵션 월간 수요확정 및 발주계획 MVP PRD

> 상태: 설계 확정본
> 작성일: 2026-08-13
> 대상: 로컬 SQLite 기반 1차 MVP
> 향후: Supabase PostgreSQL + Vercel 배포 전환

## 1. 문서 목적

본 문서는 영업 OL, SFDC 파이프라인, Bulk-deal, 과거 실적 Trend, 수급회의 결과, 전월말 재고, Open PO 및 구매조건을 활용하여 기기·옵션·부품·소모품 발주계획을 산출하고 보고자료로 출력하는 시스템의 1차 MVP 요구사항을 정의한다.

샘플 데이터는 후속 단계에서 제공되므로, MVP는 실제 파일 연계 전에도 화면 직접 입력과 샘플값으로 업무 흐름·계산·보고서 출력을 검증할 수 있어야 한다.

## 2. 제품 목표

### 목표

- 월간 발주계획 수립 업무를 하나의 웹 화면 흐름으로 통합한다.
- 담당자가 수요·재고·Open PO·마스터 데이터를 직접 입력하거나 Excel/CSV로 업로드할 수 있게 한다.
- 기기와 옵션의 발주량 계산을 일관된 규칙으로 수행한다.
- Flexibility Rule, MOQ, Lead Time, 재고 부족 등 예외를 자동으로 표시한다.
- 사장 보고에 필요한 금액·수량 비교자료를 웹, Excel, PDF로 출력한다.
- 초기에는 로컬 SQLite를 사용하되, 스키마가 확정되면 Supabase PostgreSQL로 전환 가능한 구조를 만든다.

### 비목표

MVP에는 다음을 포함하지 않는다.

- 로그인·사용자 인증·다중사용자 권한
- 실시간 SFDC·ERP·FX-LIVE API 연계
- Supplier로의 실제 발주 전송
- 선적·통관·창고 입고·PO Match·지급 실행관리
- 수급회의 일정관리 및 회의 운영 기능
- 실시간 다중 사용자 편집

## 3. 사용자 및 운영 방식

- 사용자 유형: 단일 사용자
- 로그인: 없음
- 실행환경: 사용자 로컬 컴퓨터
- 데이터베이스: SQLite 파일
- 업로드 파일: 로컬 파일 업로드 후 SQLite에 검증·저장
- 보고서: 로컬 다운로드 및 생성 이력 저장
- 향후 배포: Vercel 웹 애플리케이션 + Supabase PostgreSQL/Storage

## 4. 핵심 업무 흐름

```text
홈 대시보드
  → 발주계획 생성
  → 수요 입력
  → 수급회의 결과 반영 및 수요 확정
  → 재고·Open PO 입력
  → 마스터 검증
  → 기기·옵션 발주량 계산
  → 예외 검토 및 수동 조정
  → 보고자료 생성
  → Excel/PDF 다운로드
```

발주계획 상태는 다음과 같다.

```text
작성중 → 수요입력완료 → 재고입력완료 → 계산완료
→ 예외검토중 → 확정 → 보고자료생성
```

## 5. 기능 요구사항

### 5.1 홈 대시보드

표시 항목:

- 발주계획 목록 및 상태
- 기준월도·대상 필요월도
- 수요 확정 상태
- 재고·Open PO 입력 상태
- 마스터 검증 상태
- 계산 완료 여부
- 예외품목 수
- 총 발주금액
- 보고자료 생성 상태

동작:

- 새 발주계획 생성
- 기존 발주계획 이어서 작성
- 발주계획 상세 조회
- 마스터 관리 이동
- 보고서 다운로드

### 5.2 발주계획 생성

입력 항목:

- 발주계획명
- 기준월도
- 발주 대상 필요월도
- 전월 OL 기준월도
- 전전월 OL 기준월도
- 통화
- 계산 버전

검증:

- 기준월도와 필요월도 필수
- 동일한 이름·기준월도 조합 중복 경고
- 계산 버전 자동 부여

### 5.3 수요 입력 및 확정

입력 탭:

- OL
- SFDC 중요 파이프라인
- Bulk-deal
- 과거 실적 Trend
- 수급회의 결과
- 당월 확정수요

입력 방식:

- 웹 테이블 직접 입력
- Excel/CSV 업로드
- 업로드 전 미리보기
- 검증 오류행 다운로드

필수 검증:

- 필수 컬럼 누락
- 품목코드·기종코드 미존재
- 필요월도 형식 오류
- 수량이 숫자가 아니거나 음수인 경우
- 중복행
- 전월 대비 비정상 증감 경고

수요 상태:

- 확정
- 조건부
- 참고
- 제외

수급회의 반영:

- 회의일
- 참석자
- 확정수요
- 조정수량
- 조정사유
- Bulk-deal 사전재고 확보 여부
- 확정자

### 5.4 재고 및 Open PO 입력

재고 입력 대상:

- 기기
- 옵션
- 부품
- 소모품

재고 상태:

- 정상 가용
- 출고 대기
- 품질 보류
- 불용
- 폐기 예정

Open PO 입력 항목:

- PO 번호
- Supplier
- 품목코드
- 발주수량
- 미입고잔량
- PO 상태
- 예상 입고월도
- 납기 확정 여부

계산 반영 원칙:

- 정상 가용재고만 전월말 가용재고로 반영한다.
- 필요월도 이전 입고가 가능한 Open PO만 가용공급량으로 반영한다.
- 납기 미확정 Open PO는 위험물량으로 별도 표시한다.

### 5.5 마스터 관리

관리 대상:

- 품목
- 기종
- Supplier 및 공급지역
- BOM
- 장착율
- 평균 사용량
- MOQ 및 발주단위
- Lead Time
- Flexibility Rule
- Common품 관계
- 옵션 필수품 관계

기능:

- 직접 등록·수정·비활성화
- Excel/CSV 업로드
- 유효기간 설정
- 중복·누락 검증
- 변경 이력 기록

### 5.6 발주량 계산

계산 화면 요약:

- 총 기기 발주수량
- 총 옵션·부품·소모품 발주수량
- 총 발주금액
- Flex 예외 건수
- MOQ 과잉 건수
- 필요월도 납기 위험 건수
- 재고 부족 건수

상세 결과:

- 구분
- 품목·기종
- 확정수요
- 목표 기말재고
- 전월말 가용재고
- 가용 Open PO
- 순소요량
- MOQ
- 최종 발주량
- 단가·발주금액
- 전월 OL 대비 변동률
- 전전월 OL 대비 변동률
- 예외 상태
- 예외사유

수동 조정:

- 계산 발주량과 조정 발주량을 모두 보존한다.
- 조정 수량과 조정사유를 필수로 입력한다.
- 조정자와 조정일시를 기록한다.
- 조정 후 재계산하지 않고, 조정 결과를 보고서의 최종 발주량으로 사용한다.

### 5.7 보고자료

웹 요약:

- 당월 총 발주금액
- 기기·옵션·부품·소모품별 금액
- 전월 및 작년 동월 비교
- OL 제출량 대비 실제 발주 필요량
- 주요 증가·감소 품목
- Flex 예외
- MOQ 과잉재고
- 필요월도 납기 위험
- 차이 원인 및 담당자 의견

다운로드:

- Excel 보고서
- PDF 보고서

보고서 메타데이터:

- 발주계획 ID
- 계산 버전
- 데이터 기준월도
- 보고서 생성일시
- 최종 발주량 기준

## 6. 계산 규칙

### 6.1 확정수요

```text
확정수요
= 수급회의 확정 OL
+ 승인된 SFDC 추가수요
+ 승인된 Bulk-deal 수요
```

조건부 수요는 수급회의에서 정한 반영률을 적용한다. 참고·제외 수요는 기본 계산에 반영하지 않는다.

### 6.2 기기 순소요량

```text
기기 순소요량
= 확정수요
+ 목표 기말재고
- 전월말 가용재고
- 필요월도 이전 입고 가능한 Open PO
```

계산 결과가 음수이면 0으로 처리한다.

### 6.3 옵션·부품·소모품

선택 옵션:

```text
장착 기반 수요
= 기기 확정수요 × 장착율 × BOM 수량
```

필수 옵션:

```text
필수 옵션 수요
= 해당 기기 또는 옵션 출하수량 × 필수 BOM 수량
```

Common품:

```text
Common품 수요
= 복수 기종의 옵션 수요를 품목코드 기준 합산
```

평균 사용량 품목:

```text
평균 사용량 기반 수요
= 선택 기준기간의 평균 사용량
```

옵션 순소요량:

```text
옵션 순소요량
= 총 옵션 소요량
+ 목표 기말재고
- 전월말 가용재고
- 필요월도 이전 입고 가능한 Open PO
```

장착 기반 수요와 평균 사용량이 중복 계산되지 않도록 품목 유형별 적용 규칙을 마스터로 관리한다.

### 6.4 MOQ

```text
최종 발주량
= CEILING(순소요량 / MOQ) × MOQ
```

MOQ 적용으로 증가한 수량과 금액을 별도 산출하여 MOQ 과잉 예외로 표시한다.

### 6.5 Flexibility Rule

```text
전월 대비 변동률
= (최종 발주량 - 전월 OL) / 전월 OL

전전월 대비 변동률
= (최종 발주량 - 전전월 OL) / 전전월 OL
```

기본 허용범위:

- 전월 OL 대비 ±20%
- 전전월 OL 대비 ±30%

MVP에서는 허용범위 초과 시 발주를 자동 차단하지 않고 `예외검토` 상태로 표시한다. 사용자는 사유를 입력한 뒤 결과를 확정할 수 있다.

0인 OL의 변동률은 분모 오류를 방지하기 위해 변동률을 계산하지 않고 `기준없음`으로 표시한다.

## 7. 데이터 모델

### 발주계획·수요

- `planning_runs`: 계획 ID, 기준월도, 대상월도, 상태, 계산 버전
- `ol_demand`: OL 수량, 품목, 기종, 필요월도, 제출부서
- `sfdc_pipeline`: Deal, 고객, 수량, 예상월도, 수주 가능성
- `bulk_deals`: Bulk-deal, 수량, 고객, 상태, 선확보 여부
- `historical_actuals`: 과거 월별 출고·판매 실적
- `demand_confirmations`: 수급회의 확정수요, 상태, 조정사유, 확정일시

### 재고·공급

- `inventory_snapshots`: 전월말 재고, 상태, 창고, 품목
- `open_pos`: PO, Supplier, 발주수량, 미입고잔량, 상태, 예상입고월도
- `supplier_lead_times`: Supplier·공급지역별 Lead Time, 유효기간

### 마스터

- `items`
- `models`
- `suppliers`
- `bom_lines`
- `attachment_rates`
- `usage_averages`
- `purchase_rules`
- `common_item_relations`
- `required_option_relations`

### 계산·보고

- `calculation_results`
- `calculation_exceptions`
- `manual_adjustments`
- `reports`

모든 주요 테이블은 `id`, `created_at`, `updated_at`을 갖는다. 생성자·수정자 필드는 로그인 없는 MVP에서도 nullable로 두고 향후 Auth 확장을 대비한다.

## 8. 기술 아키텍처

```text
Next.js App Router
  → Server Actions/API Routes
  → 업무 서비스
  → Repository 계층
  → SQLite
```

구성 원칙:

- 화면 컴포넌트에서 SQL을 직접 실행하지 않는다.
- 수요확정, 재고·Open PO, 발주계산, 보고서 생성을 독립 서비스로 분리한다.
- Repository 인터페이스를 통해 SQLite 접근을 추상화한다.
- SQLite 전용 SQL 사용을 최소화한다.
- 데이터베이스 경로는 환경변수로 관리한다.
- 업로드 파일과 생성 보고서는 로컬 디렉터리에 저장한다.
- 향후 Supabase 전환 시 Repository 구현, 마이그레이션, Storage 구현만 교체한다.

## 9. 검증 및 오류 처리

### 업로드 검증

- 필수 컬럼 누락
- 품목·기종 코드 미존재
- 날짜·월도 형식 오류
- 수량 형식 오류·음수
- 중복행
- MOQ·Lead Time 누락
- BOM·장착율 누락

오류행은 저장하지 않고, 오류행·행번호·오류사유를 포함한 결과를 화면과 다운로드 파일로 제공한다.

### 계산 전 검증

- 확정수요 존재
- 재고 입력 완료 또는 없음 확정
- Open PO 입력 완료 또는 없음 확정
- 품목·기종 마스터 존재
- 필요한 BOM 존재
- MOQ·Lead Time·단가 존재
- Flexibility Rule 존재

필수 데이터 누락 시 계산을 실행하지 않고 누락 목록을 표시한다.

### 예외 수준

- 정보: 단순 참고사항
- 경고: 검토가 필요하지만 계산 가능
- 차단: 필수 마스터 누락 등 계산 불가

예외 유형에는 Flex 초과, MOQ 과잉, 납기 부족, 재고 부족, Open PO 납기 미확정, BOM·장착율·단가 누락, 비활성 품목 발주, 수요 대비 옵션 수량 불일치를 포함한다.

## 10. 테스트 및 MVP 완료 기준

### 핵심 테스트

- 기기 순소요량
- 옵션 장착율
- 필수 옵션
- Common품 합산
- 평균 사용량
- MOQ 반올림
- Flexibility Rule 판정
- 재고·Open PO 차감
- 수동 조정 이력
- Excel/CSV 오류 표시
- Excel 보고서 생성
- PDF 보고서 생성
- 계획 저장 후 재조회

### 완료 기준

사용자는 로컬 환경에서 다음을 완료할 수 있어야 한다.

1. 발주계획 생성
2. 화면 또는 Excel/CSV 수요 입력
3. 수요 확정
4. 재고와 Open PO 입력
5. 마스터 입력·업로드 및 검증
6. 기기·옵션 발주량 계산
7. 예외 확인
8. 발주량 조정 및 조정사유 저장
9. 발주금액·비교자료 확인
10. Excel/PDF 보고서 다운로드
11. SQLite 저장 데이터 재조회

## 11. 향후 Supabase 전환

- SQLite 스키마를 PostgreSQL 호환 형태로 유지한다.
- Repository 인터페이스를 유지한다.
- 마이그레이션 스크립트로 SQLite 데이터를 Supabase PostgreSQL로 이전한다.
- 로컬 파일 저장을 Supabase Storage로 교체한다.
- 환경변수만 Vercel 기준으로 전환한다.
- 이후 Supabase Auth와 RLS를 추가한다.

## 12. 확정이 필요한 후속 입력

샘플 데이터가 제공되면 다음을 실제 자료 기준으로 확정한다.

- OL·SFDC·수급회의 우선순위
- 조건부 수요 반영률
- Open PO 반영 상태
- Supplier별 실제 Lead Time
- Flex 순서와 초과 처리
- 장착율·평균 사용량 중복 처리
- MOQ·발주단위
- 단가·통화·환율·비용 포함범위
- FX-LIVE 필드 매핑

이 항목들은 MVP 화면과 계산 구조를 변경하지 않는 범위에서 설정값·마스터·연계 매핑으로 보완한다.
