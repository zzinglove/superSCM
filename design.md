# superSCM 디자인 가이드

> 참고 자료: 사용자가 제공한 `SCM Predict / Demand Analysis` HTML 시안  
> 적용 대상: `procurement-planning-mvp` 웹 애플리케이션  
> 현재 구현 기준일: 2026-08-28

## 1. 디자인 목표

이 프로젝트의 디자인은 기존의 짙은 남색·파란색 중심 프로토타입에서, 참고 HTML의 밝고 정제된 데이터 분석 제품 스타일로 전환한다.

핵심 인상은 다음과 같다.

- 기업용이지만 딱딱하지 않은 분석 도구
- 장식보다 데이터 계층과 가독성을 우선하는 화면
- 흰색 캔버스, 얇은 선, 제한된 액센트 색상
- KPI와 분석 표가 한눈에 들어오는 높은 정보 밀도
- 업무 플로우와 분석 화면이 하나의 제품처럼 느껴지는 일관성

이 디자인은 새로운 기능의 동작을 정의하지 않는다. Supabase 조회, 계산, 상태 관리 등 기존 도메인 로직은 유지하고, 정보의 구조·시각적 우선순위·컴포넌트 표현 방식을 정의한다.

## 2. 참고 디자인 분석

### 2.1 레이아웃 특징

참고 HTML은 다음과 같은 화면 구조를 사용한다.

```text
고정 사이드바
  ├─ 제품 로고와 버전
  ├─ 주요 메뉴
  └─ 지원·로그아웃

상단바
  ├─ 검색
  ├─ 수평 메뉴
  ├─ 데이터 내보내기
  ├─ 알림·설정
  └─ 사용자 프로필

스크롤 가능한 메인 캔버스
  ├─ 페이지 제목과 필터
  ├─ 차트 + KPI Bento Grid
  └─ 상세 데이터 표
```

### 2.2 시각 언어

| 요소 | 참고 디자인 방향 |
|---|---|
| 배경 | 거의 흰색에 가까운 따뜻한 배경 `#FCF9F8` |
| 표면 | 흰색 카드 `#FFFFFF` |
| 주 액센트 | Fuji Green `#00A273` |
| 보조 액센트 | 진한 녹색 `#006C4B` |
| 위험 색상 | Fuji Red `#ED1C24` |
| 본문 | 거의 검정에 가까운 `#1C1B1B` |
| 보조 텍스트 | 중간 회색 `#767676` |
| 선 | 매우 옅은 회색 `#E5E2E1` |
| 카드 모서리 | 작은 라운드 또는 거의 직각에 가까운 `4px` |
| 그림자 | 최소화. 테두리와 여백으로 계층 표현 |
| 숫자 | `JetBrains Mono` 계열의 모노스페이스 |
| UI·헤더 | `Hanken Grotesk` 계열 |

### 2.3 정보 표현 방식

- 페이지 제목은 왼쪽 액센트 선 또는 얇은 하단 경계선으로 시작한다.
- KPI는 숫자를 크게, 설명을 작고 회색으로 배치한다.
- 표의 헤더는 회색 바탕과 작은 대문자 레이블 느낌으로 표현한다.
- 숫자와 식별자는 모노스페이스로 정렬감을 만든다.
- 표 행에 마우스를 올리면 왼쪽에 Fuji Green 강조선을 표시한다.
- 위험·주의·정상 상태는 색상만 사용하지 않고 텍스트 배지나 설명을 함께 사용한다.

## 3. 현재 프로젝트에 적용된 디자인 시스템

참고 디자인의 Tailwind CDN 구조는 프로젝트에 그대로 복사하지 않는다. 현재 프로젝트 규칙에 맞춰 순수 CSS로 구현하며, 적용 파일은 다음과 같다.

```text
app/layout.tsx    → 전역 theme.css 로드
app/theme.css     → 참고 디자인 기반 오버라이드 테마
app/globals.css   → 기존 공통 스타일과 기본 클래스
```

### 3.1 색상 토큰

`app/theme.css`에서 사용하는 핵심 토큰은 다음과 같다.

```css
--scm-green: #00a273;
--scm-green-dark: #006c4b;
--scm-red: #ed1c24;
--scm-ink: #1c1b1b;
--scm-muted: #767676;
--scm-line: #e5e2e1;
--scm-subtle: #f4f4f4;
--scm-background: #fcf9f8;
```

사용 규칙:

- 주요 선택 상태·링크·버튼·강조선은 `--scm-green`을 사용한다.
- 위험·오류·삭제·검토 필요 상태는 `--scm-red`를 사용한다.
- 일반 본문은 `--scm-ink`, 보조 설명은 `--scm-muted`를 사용한다.
- 새로운 색상을 기능마다 임의로 추가하지 않는다.

### 3.2 타이포그래피

- 영문 헤더·제품명·레이블: `Hanken Grotesk`
- 숫자·코드·표 헤더: `JetBrains Mono`
- 한국어 본문: `Noto Sans KR` 폴백
- 제목은 굵기보다 크기·여백·액센트 선으로 우선순위를 만든다.
- 숫자 값은 모노스페이스와 오른쪽 정렬을 기본으로 한다.

### 3.3 형태와 여백

- 기본 카드 모서리는 `4px`이다.
- 카드에 강한 box-shadow를 넣지 않는다.
- 카드 사이 간격은 `16px`을 기본 단위로 사용한다.
- 콘텐츠 최대 폭은 `1440px`을 기준으로 한다.
- 데스크톱 좌우 여백은 약 `32px`, 모바일은 `16px`을 사용한다.
- 얇은 border와 충분한 내부 padding으로 카드의 경계를 만든다.

## 4. 화면별 디자인 규칙

## 4.1 전체 업무 화면(`/`)

### 사이드바

`components/procurement-app.tsx`의 사이드바는 참고 디자인처럼 흰색 고정 내비게이션을 사용한다.

- 폭: 데스크톱 약 `264px`
- 오른쪽에 `1px` 구분선
- 상단에 제품명과 버전 정보
- 활성 메뉴: 연한 회색 배경 + 왼쪽 Fuji Green 선
- 비활성 메뉴: 회색 텍스트, hover 시 연한 회색 배경
- 현재 업무 단계와 분석 화면을 같은 제품 내비게이션으로 표현

### 상단바

- 흰색 배경과 하단 구분선
- 현재 단계 제목과 `MONTHLY PROCUREMENT CONTROL` kicker
- 로컬·배포 상태는 작은 배지로 표현
- 검색이나 추가 도구를 넣을 때도 상단바 높이와 정렬을 유지

### 업무 단계

현재의 `DashboardStep`, `DemandStep`, `SupplyStep`, `MasterStep`, `CalculationStep`, `ReportStep`은 공통 카드·표·버튼 스타일을 사용한다.

- 단계별 제목은 페이지 헤더로 시작한다.
- 대표 KPI는 같은 높이의 카드 그리드에 배치한다.
- 입력·검증·예외 목록은 흰색 카드 안에서 그룹화한다.
- 샘플값과 실제 조회값을 시각적으로 혼동시키지 않도록 설명 문구를 둔다.
- 다음 단계·이전 단계 버튼은 화면 하단에 일관되게 배치한다.

## 4.2 분석 화면(`/analysis/*`)

### 공통 상단 구조

`app/analysis/layout.tsx`와 `components/analysis/analysis-tabs.tsx`가 담당한다.

- 왼쪽에 발주계획으로 돌아가는 링크
- 가운데 또는 왼쪽에 분석 탭
- 활성 탭은 Fuji Green 텍스트와 하단 선
- 준비되지 않은 분석은 링크 대신 잠금 상태로 표시

### 분석 헤더

`components/analysis/analysis-frame.tsx`가 담당한다.

- 작은 `ANALYSIS` kicker
- 큰 분석 제목
- 한 문장 설명
- 오른쪽 `SUPABASE LIVE` 상태 배지
- 하단 얇은 구분선

분석 제목은 “무엇을 분석하는가”를, 설명은 “어떤 기준으로 분석하는가”를 말해야 한다.

예:

```text
리드타임 격차
공급처별 마스터 리드타임과 실제 실적을 비교합니다.
```

### KPI 영역

리드타임과 소진 위험 화면 모두 다음 원칙을 따른다.

- KPI 카드는 흰색, 얇은 테두리, 작은 모서리
- 레이블은 작은 회색 텍스트
- 값은 크고 모노스페이스
- 설명은 값 아래에 배치
- 위험 수치는 빨강 또는 amber 계열 설명을 함께 사용
- KPI 개수는 화면 폭에 따라 4열에서 1열로 줄어든다.

### 데이터 표

`components/analysis/data-table.tsx`를 공통 표 컴포넌트로 사용한다.

- 표 헤더는 `--scm-subtle` 배경
- 컬럼명은 작고 명확하게 작성
- 수치 컬럼은 오른쪽 정렬
- 긴 표는 가로 스크롤 허용
- 데이터 없음과 조회 실패를 구분
- 행 hover 시 왼쪽 Fuji Green 선 표시
- 상태는 텍스트 배지 또는 사용자 지정 `render` 함수로 표현

## 5. 분석 기능별 정보 설계

### 5.1 리드타임 격차

현재 라우트:

```text
app/analysis/leadtime/page.tsx
```

표현해야 하는 정보:

- 공급처
- 국가
- 마스터 리드타임
- 표본 수
- 실적 평균
- P80
- 격차

격차 표현 규칙:

- 양수: 실제 리드타임이 마스터보다 길어 위험. 빨강 계열
- 음수 또는 0: 계획이 실적보다 짧지 않음. 녹색 계열
- `null`: `—`로 표시

### 5.2 재고 소진 위험

현재 라우트:

```text
app/analysis/stockout/page.tsx
```

표현해야 하는 정보:

- 분석 품목 수
- 소진 위험 품목 수
- 30일 이내 소진 품목 수
- 판정 불가 품목 수
- 품목코드·품목명
- 공급처
- 가용재고
- 일평균 사용량
- 계획 리드타임
- 소진 예상 일수와 날짜
- 안전·위험·판정 불가 상태
- 사용량 없음·리드타임 없음 사유

상태 표현 규칙:

```text
CRITICAL → 위험 배지
SAFE     → 안전 배지
UNKNOWN  → 판정 불가 배지 + 사유
```

화면은 소진 일수를 임의로 계산하거나 누락값을 숫자로 대체하지 않는다. 계산과 판정은 `analytics.v_stockout_risk`가 제공하고 화면은 결과를 표현한다.

## 6. 컴포넌트 재사용 규칙

### 재사용해야 하는 컴포넌트

- `AnalysisFrame`: 분석 화면 제목·설명·상태 배지
- `DataTable`: 모든 분석 결과 표
- `formatNumber`: 숫자와 `null` 표현
- `StepFrame`: 업무 단계 하단 이동 영역
- `card`, `metric`, `tag`, `button`: 공통 CSS 클래스

### 기능별로 분리해야 하는 것

- DB 조회 함수: `lib/scm.ts`
- DB 컬럼 정규화: `lib/scm-model.ts`
- 상태별 배지와 사유 표현: 각 분석 페이지 또는 별도 분석 UI 컴포넌트
- 실제 업무 계산식: Supabase 뷰 또는 순수 도메인 함수

화면마다 표를 새로 만들거나, Supabase 컬럼명을 JSX 안에서 직접 해석하지 않는다.

## 7. 반응형 규칙

### 데스크톱

- 사이드바 표시
- 분석 KPI 4열 또는 3열
- 분석 헤더의 제목과 상태 배지를 양쪽에 배치
- 표는 가능한 한 한 화면에 많은 컬럼을 표시

### 모바일

- 사이드바 숨김
- 좌우 padding `16px`
- KPI는 1열 또는 2열
- 분석 탭은 가로 스크롤
- 표는 가로 스크롤을 허용하되 핵심 컬럼을 앞에 둔다.
- 제목은 데스크톱보다 작게 줄인다.

모바일에서 표를 억지로 카드 목록으로 바꾸지 않는다. 분석 표는 숫자 비교가 목적이므로 가로 스크롤이 더 적합하다.

## 8. 상태와 접근성

- 색상만으로 위험 상태를 전달하지 않는다. `위험`, `안전`, `판정 불가` 텍스트를 함께 표시한다.
- 버튼은 실제 버튼 요소를 사용한다.
- 카드처럼 동작하는 영역은 키보드 `Enter`와 `Space` 입력을 지원한다.
- 링크와 버튼의 hover 상태뿐 아니라 현재 활성 상태도 명확히 표시한다.
- 입력 요소에는 연결된 `label`을 둔다.
- 표의 헤더는 `th`로 작성하고, 빈 결과에는 사용자가 다음에 확인할 위치를 안내한다.
- 조회 실패와 빈 결과를 같은 문구로 처리하지 않는다.

## 9. 참고 HTML과 현재 프로젝트의 차이

| 항목 | 참고 HTML | 현재 프로젝트 적용 방향 |
|---|---|---|
| 스타일 기술 | Tailwind CDN | 기존 순수 CSS 유지 |
| 언어 | 영문 중심 | 업무 화면은 한국어 중심 |
| 차트 | 막대 차트 포함 | 현재 데이터 연결 범위 안에서 단계적으로 추가 |
| 제품명 | SCM Predict | 월간 발주계획 유지 |
| 데이터 | 정적 예시 | Supabase 분석 뷰 사용 |
| 메뉴 | Dashboard·Engine·Inventory | 기존 업무 플로우·분석 메뉴 유지 |
| 사용자 기능 | 검색·내보내기·알림 | 실제 기능이 생길 때만 추가 |
| 프로필 이미지 | 포함 | 현재 기능상 필요하지 않으면 추가하지 않음 |

참고 디자인은 시각적 방향을 제공하는 자료이지, 존재하지 않는 기능을 추가하라는 요구사항은 아니다. 기능이 없는 검색·알림·내보내기 버튼을 장식용으로 먼저 만들지 않는다.

## 10. 디자인 변경 시 체크리스트

- [ ] `app/theme.css`의 색상 토큰을 우선 사용했는가?
- [ ] 기존 `globals.css`와 중복되거나 충돌하는 스타일이 없는가?
- [ ] 카드 모서리와 테두리가 다른 화면과 일관적인가?
- [ ] 숫자와 코드가 오른쪽 또는 모노스페이스로 정렬되는가?
- [ ] 위험·정상·판정 불가 상태가 텍스트로도 구분되는가?
- [ ] 모바일에서 콘텐츠가 잘리지 않고 표를 스크롤할 수 있는가?
- [ ] 조회 오류와 빈 결과를 구분하는가?
- [ ] 기능이 없는 버튼이나 메뉴를 임의로 추가하지 않았는가?
- [ ] `npm run test`가 통과하는가?
- [ ] `npm run build`가 통과하는가?

## 11. 디자인 원칙 요약

```text
데이터가 주인공이다.
색상은 강조에만 쓴다.
선과 여백으로 구조를 만든다.
숫자는 정확하고 정렬되어 보여야 한다.
오류와 빈 결과를 구분한다.
기능 없는 장식은 추가하지 않는다.
새 화면도 기존 분석 컴포넌트를 재사용한다.
```

## 8. 최종 디자인 요구사항 업데이트

최종 화면은 참고 HTML의 구조를 그대로 따르지 않고, 기존 superSCM 화면의 레이아웃과 사용성을 기준으로 유지한다.

- 사이드바, 상단바, 콘텐츠 폭, 카드 모서리, 카드 내부 여백, KPI 크기, 표 간격은 기존 화면 기준을 사용한다.
- 기존의 카드형 레이아웃과 분석 화면의 정보 밀도를 유지한다.
- 새 디자인 시스템의 색상 토큰과 상태 표현만 적용한다. 색상은 컴포넌트에 직접 작성하지 않고 CSS 토큰으로 관리한다.
- SAFE, WARNING, CRITICAL, CALCULATION_UNAVAILABLE 상태는 공통 Badge로 표현한다.
- 계산 불가 값은 숫자 0으로 대체하지 않고 — + reason_code 형식으로 표시한다.
- Lead Time과 Stockout 화면은 동일한 기존 레이아웃을 공유하되, 데이터 조회·계산 로직은 변경하지 않는다.
- 신규 URL(/user/dashboard, /user/analysis/leadtime, /user/analysis/stockout)에서도 기존 화면과 같은 시각적 계층을 유지한다.
